<div class="mt-4">
  <nav>

    <div data-controller="menu" class="dropdown relative px-4 cursor-pointer">
      <div data-action="click->menu#toggle" class="md:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 5.25h16.5m-16.5 4.5h16.5m-16.5 4.5h16.5m-16.5 4.5h16.5"/>
        </svg>
      </div>
      <ul class="absolute flex flex-col gap-x-1 gap-y-1 hidden  z-40 bg-white rounded px-2 py-1 md:static md:flex md:flex-row md:flex-nowrap md:block md:bg-inherit" data-menu-target="toggleable">
        <%= link_to "Accounts", Account, class: "border-b text-blue-700 font-bold md:btn-primary" %>
        <%= link_to "Categories", Category, class: "border-b text-blue-700 font-bold md:btn-primary" %>
        <%= link_to "Reporting", "/reporting", class: "border-b text-blue-700 font-bold md:btn-primary" %>

        <% Account.where(in_menu: true).each do |account| %>
          <%= link_to account.name, account, class:"border-b text-blue-700 font-bold md:btn-primary" %>
        <% end %>
      </ul>
    </div>

  </nav>
</div>
