<%= turbo_stream.update "flash" do %>
<% flash.each do |flash_type, message| %>
  <div class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block"><%= message %></div>
<% end %>
<% end %>
