<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= yield :title %>

    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1.2">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-100 text-sm ml-5 md:ml-10  md:text-base grid grid-rows-auto">
    <header>
      <div class="">
        <%= render "shared/top_menu" %>
      </div>
    </header>
    <div class="">
      <div class="py-4">
        <div id="flash"></div>
          <main role="main" class="grid grid-cols-10 md:gap-x-3">
            <div class="col-span-10 text-2xl md:text-3xl font-bold mb-3 justify-self-center">
                <div >
                  <%= yield :title %>
                </div>
            </div>
            <div class="col-span-10 md:col-span-8 -100">
              <%= yield %>
                <%# <div class="bg-yellow-200">
                  TABLE AREA -----------------------------------------------------------------------------------------------------------------------------------------
                </div> %>
              </div>
              <div class="col-span-4  md:col-span-2">
                  <div class="-200">
                    <%= yield :right %>

                  </div>
                </div>
              </main>
            </div>
          </div>
        </body>
      </html>
